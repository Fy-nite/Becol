#include <stddef.h>
#include "tokenizer.h"
#include "asttree.h"
#include "tokens.h"


ASTTreeNode* BecolGenASTTree(char* prog) {
    //Program* program = BecolGetProgram(prog); // get instance of program so that we can get tokens
    return NULL;
}
